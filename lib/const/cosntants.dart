import 'package:flutter/material.dart';

// =================== DARK MODE COLORS ===================

// Background for cards, tiles, panels
const cardBackgroundColor = Color(0xFF21222d); // Dark grey-blue

// Primary color for accents, buttons, selection
const primaryColor = Color(0xFF2697ff); // Soft Indigo-Violet (modern & vibrant)

// Secondary background, sidebar, or containers
const secondaryColor = Color(0xFFffffff); // Deep charcoal

// Overall app background
const backgroundColor = Color(0xFF15131c); // Pure dark (Material Dark)

// Selected item highlight (slightly lighter than card background)
const selectionColor = Color(0xFF88b2ac); // Muted bluish-gray

// Border or divider color
const borderColor = Color(0xFF3C3F58); // For outlines, subtle lines

// =================== TEXT COLORS ===================

// Light text for dark background
const textPrimaryColor = Colors.white;
const textSecondaryColor = Colors.white70;

// =================== SPACING ===================

const defaultPadding = 20.0;
